# Project Structure

This scaffold is arranged as follows:

- `hardware/` PCB, revision, and mechanical assets
- `firmware/` embedded code and tests
- `software/` companion applications
- `models/` 3D exports
- `docs/` safety, assembly, wiring, and BOM documents
- `examples/` small validation projects
- `.github/` issue templates, PR template, workflow checks
